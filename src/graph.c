#include "pathfinder.h"


int main(void) {
    t_graph graph;
    graph.mtx = malloc(sizeof(mtx));
    graph.mtx = mtx;
    graph.vertex_number = VERTEX_NUMBER;
    mx_printint(VERTEX_NUMBER);
    mx_printchar('\n');

    test(graph);
    return 0;
}
